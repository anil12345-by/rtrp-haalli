also in window credentials there is not github of mine

create window credentials like i have said at night right sam elike that create a token and put it in windows creditanls in username and in place of password put token
fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (main)
$ git version
git version 2.52.0.windows.1

fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (main)
$ git config --global user.name"anil12345-by"

fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (main)
$ git config --global user.name"aniladitya11d@gmail.com"

fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (main)
$ git login
git: 'login' is not a git command. See 'git --help'.

The most similar command is
        column

fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (main)
$ git clone https://github.com/deepthisagar7/library-management.git
Cloning into 'library-management'...
remote: Enumerating objects: 34, done.
remote: Counting objects: 100% (34/34), done.
remote: Compressing objects: 100% (22/22), done.
remote: Total 34 (delta 2), reused 30 (delta 0), pack-reused 0 (from 0)
Receiving objects: 100% (34/34), 5.43 KiB | 1.09 MiB/s, done.
Resolving deltas: 100% (2/2), done.

fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (main)
$ git init
Initialized empty Git repository in C:/Users/fs/Desktop/library-management/.git/

fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (master)
$ git add .
warning: adding embedded git repository: library-management
hint: You've added another git repository inside your current repository.
hint: Clones of the outer repository will not contain the contents of
hint: the embedded repository and will not know how to obtain it.
hint: If you meant to add a submodule, use:
hint:
hint:   git submodule add <url> library-management
hint:
hint: If you added this path by mistake, you can remove it from the
hint: index with:
hint:
hint:   git rm --cached library-management
hint:
hint: See "git help submodule" for more information.
hint: Disable this message with "git config set advice.addEmbeddedRepo false"

fs@DESKTOP-P44M87A MINGW64 ~/Desktop/library-management (master)
i need to import files from eclipse to git it seems
i am doing part 2 first for that i should  repository file fro eclipse to mu git and then perform operations
i should not clone it
i should clone it maven ig idk
yes you have to clone the github repo link into your desktop folder then open maven see at top file import and click on general and there click on existing projects into workspace then clikc on browse and slect your folder finsish



if  I open the folder and select through browse its not showing next

1. First check the folder structure

Inside the folder they selected, it must contain pom.xml.

For example:

Desktop
└── library-management
    ├── pom.xml        ← MUST BE HERE
    ├── src
    └── target

If pom.xml is not directly inside the selected folder, Eclipse won't recognize it as the Maven project.

2. Clone correctly

In CMD:

cd Desktop
git clone https://github.com/deepthisagar7/library-management.git

Then:

cd library-management
dir

They should see:

pom.xml
src
3. Eclipse import

In Eclipse:

File
 ↓
Import
 ↓
General
 ↓
Existing Projects into Workspace
 ↓
Browse
 ↓
Select the library-management folder

Then Eclipse should show something like:

Projects:
☑ library-management

Click:

Finish





















solve and give i should import files from eclipse and do in git


should i give steps or directly files??
give steps from starting how to import from eclipse 

how is the invigilation and can we open gpt?
